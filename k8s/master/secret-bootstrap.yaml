# Pre-created empty Secret — the master sidecar patches this with the live token.
# Web and worker pods mount PACKA_*_BOOTSTRAP_TOKEN from here.
apiVersion: v1
kind: Secret
metadata:
  name: packa-bootstrap-token
  namespace: packa
type: Opaque
stringData:
  token: ""
