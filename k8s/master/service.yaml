apiVersion: v1
kind: Service
metadata:
  name: master
  namespace: packa
spec:
  selector:
    app: packa-master
  ports:
    - name: api
      port: 9000
      targetPort: 9000
  clusterIP: None   # headless — workers connect by DNS name "master.packa.svc.cluster.local"
