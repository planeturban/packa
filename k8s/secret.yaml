# Optional — set credentials here or via environment variables.
# Values must be base64-encoded: echo -n 'value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: packa-secret
  namespace: packa
type: Opaque
data:
  # web-username: YWRtaW4=      # admin
  # web-password: c2VjcmV0      # secret
