# Optional Ingress — uncomment and fill in host/TLS if you use an ingress controller.
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: web
#   namespace: packa
#   annotations:
#     nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
# spec:
#   ingressClassName: nginx
#   rules:
#     - host: packa.example.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: web
#                 port:
#                   number: 8080
#   tls:
#     - hosts:
#         - packa.example.com
#       secretName: packa-tls
