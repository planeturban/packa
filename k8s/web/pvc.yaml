apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-web
  namespace: packa
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 128Mi
