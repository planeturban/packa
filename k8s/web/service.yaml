apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: packa
  annotations:
    metallb.universe.tf/allow-shared-ip: packa
spec:
  selector:
    app: packa-web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: LoadBalancer
  loadBalancerIP: 10.0.100.72
