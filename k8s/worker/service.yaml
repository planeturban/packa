# One Service per worker — copy and rename alongside the StatefulSet.
apiVersion: v1
kind: Service
metadata:
  name: worker-01
  namespace: packa
spec:
  selector:
    app: packa-worker
    worker-id: worker-01
  ports:
    - name: api
      port: 8000
      targetPort: 8000
  clusterIP: None
